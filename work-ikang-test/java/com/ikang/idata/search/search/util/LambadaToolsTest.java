package com.ikang.idata.search.search.util;

import org.junit.jupiter.api.Test;

import java.util.function.BiConsumer;
import java.util.function.Consumer;

import static org.mockito.Mockito.mock;

class LambadaToolsTest {

    @Test
    void testForEachWithIndex() {
        // Setup
//        final BiConsumer<String, Integer> mockBiConsumer = mock(BiConsumer.class);

        // Run the test
//        final Consumer<String> result = LambadaTools.forEachWithIndex(mockBiConsumer);

        // Verify the results
    }

    @Test
    void testMain() {
        // Setup
        // Run the test
//        LambadaTools.main(new String[]{"args"});

        // Verify the results
    }
}
